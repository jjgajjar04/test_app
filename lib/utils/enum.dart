enum AuthState {
  initial,
  authenticating,
  authenticated,
  unauthenticated,
}

enum ApiState {
  initial,
  loading,
  loaded,
  error,
}

enum PageState {
  initial,
  loading,
  loaded,
  error,
}
